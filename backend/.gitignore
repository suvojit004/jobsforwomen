node_modules
# Keep environment variables out of version control
.env

/generated/prisma

# Local-disk file storage (dev only -- production uses a Render Persistent Disk)
/uploads

# Output of `npm run email:preview` -- rendered HTML, not source
/email-previews
